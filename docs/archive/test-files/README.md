# Archived test JSON samples

JSON files in this folder are **frozen historical samples**. They may reference legacy `strategy_id` values (for example `peer_tutoring_bilingual`) that are no longer in `strategies_pack_v2` but remain valid for regression or documentation of older outputs.

Do not assume these ids match the current strategy pack without checking `strategies_pack_v2/_index.json`.
